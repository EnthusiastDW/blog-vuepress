---
title: Spring AI
date: 2026-02-11 11:36:48
permalink: /pages/spring-ai/
categories:
  - java
tags:
  - AI
  - spring
author: 
  name: Great Deng
  link: https://github.com/EnthusiastDW
---
> https://spring.io/projects/spring-ai

Spring AI 是一个用于 AI 工程的应用框架。它的目标是将在 AI 领域应用 Spring 生态系统设计原则，如可移植性和模块化设计，并推动将 POJOs 作为应用程序的构建模块引入 AI 领域。

![](https://images.ctfassets.net/mnrwi97vnhts/4mda205vy509Dx3vGkMwFr/af520e66dc79fb80cd1bc129a11d6d23/spring-ai-integration-diagram-3.svg)

其核心，Spring AI 解决了 AI 集成的根本挑战：将您的企业数据和 API 与 AI 模型连接起来。

## 特性

- 支持所有主要的模型提供商，如：Anthropic, OpenAI, Microsoft, Amazon, Google, and Ollama。
  支持的模型类型：
  - 聊天补全
  - 嵌入
  - 文生图
  - 音频转录
  - 文本转语音
  - Moderation
- 便携式 API支持跨 AI 提供商，包括同步和流式 API 选项。还可以访问特定模型的特性。
- 结构化输出——AI 模型输出映射到 POJOs
- 支持所有主要的向量数据库，如 Apache Cassandra、Azure Vector Search、Chroma、Milvus、MongoDB Atlas、Neo4j、Oracle、PostgreSQL/PGVector、PineCone、Qdrant、Redis 和 Weaviate。
- 跨向量存储提供商的便携式 API，包括一种新颖的类似 SQL 的元数据过滤 API。
- 工具/函数调用（[Tools/Function Calling](https://docs.spring.io/spring-ai/reference/api/functions.html)） - 允许模型请求执行客户端工具和函数，从而按需访问必要的实时信息。
- 可观察性（[Observability](https://docs.spring.io/spring-ai/reference/observability/index.html)） - 提供有关人工智能相关操作的观测。
- 数据工程文档注入 [ETL 框架](https://docs.spring.io/spring-ai/reference/api/etl-pipeline.html)。
- [AI 模型评估](https://docs.spring.io/spring-ai/reference/api/testing.html) - 用于评估生成内容并防止产生幻觉响应的工具。
- [ChatClient API](https://docs.spring.io/spring-ai/reference/api/chatclient.html) - 用于与 AI 聊天模型通信的Fluent API，类似于 WebClient 和 RestClient API。
- [Advisors API ](https://docs.spring.io/spring-ai/reference/api/advisors.html)- 封装了反复出现的生成式 AI 模式，转换发送到模型和来自模型的数据，并为跨多种模型和使用场景提供了便携性。
- 支持聊天[对话记忆](https://docs.spring.io/spring-ai/reference/api/chatclient.html#_chat_memory)和[检索增强生成](https://docs.spring.io/spring-ai/reference/api/chatclient.html#_retrieval_augmented_generation)（RAG）。
- Spring Boot 自动配置所有 AI 模型和向量存储 - 使用 [start.spring.io](https://start.spring.io/) 选择所需的模型或向量存储。

这个功能集让你能够实现诸如" `Q&A over your documentation` "或" `Chat with your documentation.` "等常见用例

## 入门

你可以从以下简单几部开始：

1. 创建一个包含 Spring AI OpenAI 启动器的 Spring Boot Web 应用程序。这个 [Spring Initializr](https://start.spring.io/#!type=maven-project&language=java&platformVersion=3.3.4&packaging=jar&jvmVersion=17&groupId=spring.ai.example&artifactId=spring-ai-demo&name=spring-ai-demo&description=Spring%20AI%20%2C%20getting%20started%20example%2C%20using%20Open%20AI&packageName=spring.ai.example.spring-ai-demo&dependencies=web,spring-ai-openai) 链接可以帮助你生成应用程序。（使用 [start.spring.io](https://start.spring.io/)，你可以选择任何想要在你的新应用程序中使用的 AI 模型或向量存储）。
2. 在application.properties中添加OpenAI key

```properties
	spring.ai.openai.api-key=<YOUR OPENAI KEY>
```

3. 复制以下代码到`SpringAiDemoApplication`中

```java
@Bean public CommandLineRunner runner(ChatClient.Builder builder) { 
	return args -> { ChatClient chatClient = builder.build(); 
		String response = chatClient.prompt("Tell me a joke").call().content();  
		System.out.println(response); 
	}; 
}
```

3. 启动程序

```bat
./mvnw spring-boot:run
```
